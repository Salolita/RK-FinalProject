import './Auth.css'
import { 
    Box,
    Tabs,
    Tab
} from '@mui/material';

const Auth = () => (
    <div className="auth-panel">
        <h4>
            Log in
        </h4>

        {/* <Box sx={{ mt: 2 }}>
            {tabValue === 0 ? <Login /> : <SignUp />}
        </Box> */}
    </div>
)

export default Auth